#include <catch2/catch_test_macros.hpp>

// TMPL-11 / TMPL-13 — SqliteTemplateRepository is implemented in Plan 04.
// Real assertions replace this stub once sqlite_template_repository.{hpp,cpp} exist.
TEST_CASE("SqliteTemplateRepository: placeholder — implementation in Plan 04",
          "[storage.template_repo]") {
    REQUIRE(1 == 1);
}
