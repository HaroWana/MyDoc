#include <catch2/catch_test_macros.hpp>

// TMPL-04 / TMPL-07 — PlainTextDocumentReader is implemented in Plan 03.
// Real assertions replace this stub once plain_text_document_reader.{hpp,cpp} exist.
TEST_CASE("PlainTextDocumentReader: placeholder — implementation in Plan 03",
          "[formats.plain_text]") {
    REQUIRE(1 == 1);
}
