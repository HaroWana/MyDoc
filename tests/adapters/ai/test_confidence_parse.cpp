#include <catch2/catch_test_macros.hpp>

#include "domain/confidence.hpp"

TEST_CASE("Confidence enum round-trips through string conversion (high/medium/low/manual)",
          "[adapters.ai][confidence][skip-until-impl]") {
    REQUIRE(true);
}
