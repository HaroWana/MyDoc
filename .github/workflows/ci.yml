name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  placeholder:
    name: Wave 1 stub — full matrix lands in Plan 05
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Announce
        run: echo "Phase 0 Plan 01 wired CI placeholder; full matrix delivered in Plan 05."
